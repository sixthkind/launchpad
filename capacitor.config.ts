import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sixthkind.launchpad',
  appName: 'Launchpad',
  webDir: '.output/public'
};

export default config;
